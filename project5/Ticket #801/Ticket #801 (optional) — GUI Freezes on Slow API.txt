Client Note:
“When the API takes 5 seconds, the GUI window
hangs. Users think it crashed.”

import tkinter as tk
import requests
def get_weather():
city = entry.get()
data =
requests.get(f"https://example.com/weather/{city}").tex
t
label.config(text=data)
root = tk.Tk()
entry = tk.Entry(root); entry.pack()
button = tk.Button(root, text="Get Weather",
command=get_weather); button.pack()
label = tk.Label(root, text=""); label.pack()
root.mainloop()